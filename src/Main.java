import graphPackage.Edge.OrientedEdge;
import graphPackage.Node.Node;
import graphPackage.OrientedGraph;
import thread.ThreadProva;

import java.util.ArrayList;
import java.util.List;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        // Press Alt+Invio with your caret at the highlighted text to see how
        // IntelliJ IDEA suggests fixing it.

        //ThreadProva p = new ThreadProva();
        //p.create();

            // Create nodes


    }
}